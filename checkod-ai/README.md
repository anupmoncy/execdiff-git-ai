# DEPRECATED: checkod-ai

**This package has been renamed to [execdiff-git-ai](https://pypi.org/project/execdiff-git-ai/).**

## Migration

To continue using this tool, please install the new package:

```bash
pip install execdiff-git-ai
```

## Why?

The project is now maintained under the new name for clarity and future development.

## CLI Notice

Any usage of `checkod scan` will now show a migration message and exit.

---
